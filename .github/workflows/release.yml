name: Release on tag
on:
  push:
    tags:
      - 'v*'

jobs:
  build_web:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Sync package.json version from tag
        run: npm version "${GITHUB_REF_NAME#v}" --no-git-tag-version
      - run: npm run build
      - uses: actions/upload-artifact@v4
        with:
          name: web-dist
          path: dist/web

  deploy_firebase:
    needs: build_web
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: web-dist
          path: dist/web
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_MAKE_A_SET }}
          channelId: live
          projectId: make-a-set

  build_desktop:
    needs: build_web
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - uses: actions/download-artifact@v4
        with:
          name: web-dist
          path: dist/web

      # Prepare desktop build from existing web build
      - run: npm run build:desktop:prepare

      # Build Linux and Windows in sequence (nw-builder downloads NW.js)
      - run: node scripts/build-desktop.ts --platform=linux
      - run: node scripts/build-desktop.ts --platform=win

      # Package Linux AppImage
      - run: npm run package:appimage
        env:
          ARCH: x86_64

      # Zip Windows build
      - name: Zip Windows build
        run: cd dist/desktop/win32 && zip -r ../all-or-nothing-${{ github.ref_name }}-windows-x64.zip .

      # Rename AppImage to include tag version
      - name: Rename AppImage
        run: mv dist/desktop/all-or-nothing-*-x86_64.AppImage dist/desktop/all-or-nothing-${{ github.ref_name }}-linux-x86_64.AppImage

      # Create GitHub Release with both assets
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            dist/desktop/all-or-nothing-${{ github.ref_name }}-linux-x86_64.AppImage
            dist/desktop/all-or-nothing-${{ github.ref_name }}-windows-x64.zip

      # Upload desktop artifacts for itch.io deployment
      - uses: actions/upload-artifact@v4
        with:
          name: desktop-linux
          path: dist/desktop/all-or-nothing-${{ github.ref_name }}-linux-x86_64.AppImage
      - uses: actions/upload-artifact@v4
        with:
          name: desktop-windows
          path: dist/desktop/all-or-nothing-${{ github.ref_name }}-windows-x64.zip

  build_web_itchio:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Sync package.json version from tag
        run: npm version "${GITHUB_REF_NAME#v}" --no-git-tag-version
      - run: npm run build
        env:
          VITE_BASE: './'
      - uses: actions/upload-artifact@v4
        with:
          name: web-dist-itchio
          path: dist/web

  deploy_itchio:
    needs: [build_web_itchio, build_desktop]
    runs-on: ubuntu-latest
    steps:
      - name: Download web build
        uses: actions/download-artifact@v4
        with:
          name: web-dist-itchio
          path: web
      - name: Download Linux build
        uses: actions/download-artifact@v4
        with:
          name: desktop-linux
          path: desktop-linux
      - name: Download Windows build
        uses: actions/download-artifact@v4
        with:
          name: desktop-windows
          path: desktop-windows

      - name: Install butler
        run: |
          curl -L -o butler.zip https://broth.itch.zone/butler/linux-amd64/LATEST/archive/default
          unzip butler.zip
          chmod +x butler
          ./butler -V

      - name: Push web build
        run: ./butler push web deegeegames/all-or-nothing:html5 --userversion "${{ github.ref_name }}"
        env:
          BUTLER_API_KEY: ${{ secrets.BUTLER_API_KEY }}
      - name: Push Linux build
        run: ./butler push desktop-linux deegeegames/all-or-nothing:linux --userversion "${{ github.ref_name }}"
        env:
          BUTLER_API_KEY: ${{ secrets.BUTLER_API_KEY }}
      - name: Push Windows build
        run: ./butler push desktop-windows deegeegames/all-or-nothing:windows --userversion "${{ github.ref_name }}"
        env:
          BUTLER_API_KEY: ${{ secrets.BUTLER_API_KEY }}

  sync_version:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: master
      - name: Update package.json version from tag
        run: npm version "${GITHUB_REF_NAME#v}" --no-git-tag-version
      - name: Commit and push version update
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package.json
          git diff --staged --quiet && echo "package.json already up to date" && exit 0
          git commit -m "chore: sync version to ${GITHUB_REF_NAME}"
          git push origin master
